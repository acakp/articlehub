# articlehub
минисервис по отображению, созданию и редактированию публикаций

# Установка

1. клонировать репозиторий и зайти в него
2. создать venv: `python -m venv venv`
3. установить зависимости: `pip -r requerments.txt`
